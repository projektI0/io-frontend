import React from 'react';

const Footer = () => {
    return (
        <div className={"flex bg-gray-100 text-sm font-body justify-center py-2 text-gray-800"}>
            @Made by CD Project Blue team
        </div>
    );
};

export default Footer;
